---
- name: reloads udev rules
  shell: |
    udevadm control --reload-rules &&  udevadm trigger

- name: applies usbfs-memory-increase service
  systemd:
    daemon_reload: true
    name: usbfs-memory-increase
    enabled: true
    state: restarted
